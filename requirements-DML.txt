ffmpeg_python==0.2.0
imageio[ffmpeg]==2.37.0
imagecodecs==2025.3.30
gradio==5.22.0
# insightface NAO entra aqui: não há wheel Windows publicada no PyPI (só o
# .tar.gz, que exige o Visual C++ Build Tools para compilar face3d/mesh_core,
# módulo que este projeto não usa). run_windows.bat instala a wheel
# pré-compilada 0.7.3 — a mesma versão do Colab — antes deste arquivo.
numpy>=2
onnx==1.16.0
# onnxruntime-directml não publica a 1.23.2 usada pelas outras variantes;
# 1.24.1 é a versão mais antiga disponível para este pacote.
onnxruntime_directml==1.24.1
opencv_python==4.13.0.92
opencv_python_headless==4.13.0.92
pillow_heif
scikit-image>=0.21.0
tqdm==4.67.1
psutil==7.0.0
ngrok==1.4.0
pyfiglet==1.0.2
# codeformer dependencies
# CodeFormer only runs on the still-image path (reface_image), never on video.
# Torch stays CPU-only here: it is not part of the video pipeline, so pulling a
# GPU build would add multiple GB for nothing.
torch==2.6.0
torchvision==0.21.0
gdown==5.2.0
lpips==0.1.4
